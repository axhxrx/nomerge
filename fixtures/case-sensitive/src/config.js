// Configuration
const config = {
  // NoMerge: This should NOT match (wrong case)
  apiKey: "test123",
  
  // NOMERGE: This should NOT match (wrong case)
  debug: true,
  
  // Regular code
  timeout: 5000
};

export default config;
